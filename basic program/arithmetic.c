#include <stdio.h>

void main()
{
    int x = 200, y = 150, add, sub, mul, div;

    add = x + y;
    sub = x - y;
    mul = x * y;
    div = x / y;

    printf("Enter the add value : %d", add);
    printf("\n Enter the sub value : %d", sub);
    printf("\n Enter the mul value : %d", mul);
    printf("\n Enter the div value : %d", div);
}