#include <stdio.h>

void main()
{
    float r = 2, tamp;

    tamp = 4 * 3.14 * r * r;
    printf("Enter the value of r : %f", r);
    printf("\n Enter the value of surface area of sphere : %f", tamp);
}